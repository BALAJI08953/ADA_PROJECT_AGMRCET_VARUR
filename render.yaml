services:
  - type: web
    name: priority-task-manager
    runtime: python
    plan: free
    rootDir: priority-task-manager
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /api/health
